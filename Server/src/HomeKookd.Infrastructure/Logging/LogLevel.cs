namespace HomeKookd.Infrastructure.Logging
{
    public enum LogLevel
    {
        Fatal = 600,
        Error = 500,
        Warn = 400,
        Info = 300,
        Debug = 200,
        Trace = 100
    }
}